const questHelper = {
  quests: [
    { key: "first", description: "First" },
    { key: "second", description: "Second" },
    { key: "mixed", description: "Mixed" }
  ]
} as const;

export default questHelper;
